# Loom Guide

Official operating guidance for agents running inside Loom.

This directory is the packaged snapshot for `loom guide`. The source repository is:

```text
git@github.com:wyw-ai/loom-guide.git
```

Use `loom guide update` to refresh the local guide cache from that repository.
