[package]
name = "context-layer-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
proto = { workspace = true }
anyhow = { workspace = true }
inventory = { workspace = true }
serde = { workspace = true }
# B4: plugin factory signature carries the config envelope
serde_json = { workspace = true }

[features]
# Test-only helpers (e.g. `assert_skip_not_truncate`). Enabled via
# dev-dependencies; never enabled in production builds.
test-helpers = []

[dev-dependencies]
serde_json = { workspace = true }

[lints]
workspace = true
