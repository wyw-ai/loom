[package]
name = "plugin-memory"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
# Contract layer only — this crate must NOT depend on agent-runtime
# (compile-time proof that the official memory plugin uses nothing
# beyond the public ContextResource contract; ARCH iter2 §3).
context-layer-core = { workspace = true }
proto = { workspace = true }
anyhow = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
chrono = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
uuid = { workspace = true }
tempfile = { workspace = true }
context-layer-core = { workspace = true, features = ["test-helpers"] }

[lints]
workspace = true
