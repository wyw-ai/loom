{
  "actor": {
    "id": "actor_feedback_fix_orchestrator",
    "kind": "agent",
    "displayName": "\u53cd\u9988\u4fee\u590d\u7f16\u6392"
  },
  "transport": {
    "kind": "interactive_command",
    "command": "claude",
    "args": [],
    "env": {},
    "model": "claude-sonnet-4.6",
    "provider": {
      "kind": "claude",
      "settings": {
        "mode": "actor_profile"
      }
    },
    "interactive": {
      "session": {
        "idStrategy": "joi_uuid_per_scope",
        "newArgs": [
          "--print",
          "--permission-mode",
          "bypassPermissions",
          "{prompt}",
          "--session-id",
          "{session_id}"
        ],
        "resumeArgs": [
          "--print",
          "--permission-mode",
          "bypassPermissions",
          "{prompt}",
          "--resume",
          "{session_id}"
        ],
        "onMissing": "create",
        "onSignatureChanged": "create",
        "onResumeFailed": "fail"
      },
      "prompt": {
        "template": "{joi_envelope}",
        "completionContract": {
          "sentinel": "__JOI_DONE__",
          "instruction": "When your final user-visible answer is complete, output __JOI_DONE__ on a line by itself. Do not output anything after it."
        }
      },
      "completion": {
        "detect": "sentinel",
        "stripSentinel": true,
        "idleTimeoutMs": 30000,
        "maxTurnMs": 1800000
      },
      "output": {
        "stripAnsi": true,
        "stderr": "trace",
        "streamPartial": true
      },
      "kill": {
        "onComplete": {
          "action": "sigterm",
          "graceMs": 3000,
          "fallback": "sigkill"
        },
        "onCancel": {
          "action": "sigterm",
          "graceMs": 1000,
          "fallback": "sigkill"
        },
        "onTimeout": {
          "action": "sigkill"
        }
      }
    }
  },
  "autostart": false,
  "bundle": {
    "source": "data/agents/feedback-fix-orchestrator/bundle",
    "version": "v1",
    "installMode": "copy",
    "root": "{agent.root}/bundles",
    "current": "{agent.root}/bundles/current"
  },
  "handoff": {
    "triggerPromptPrefix": "/feedback-fix-orchestrator\n",
    "applyOn": "every-turn"
  },
  "promptTemplate": {
    "activeSkill": "feedback-fix-orchestrator",
    "everyTurnPrefix": [
      "[joi handoff v1]",
      "scope_kind: {scope.kind}",
      "scope_id: {scope.id}",
      "actor_id: {actor.id}",
      "from_actor: {trigger.actor_id}",
      "trigger_event_id: {trigger.id}",
      "channel_id: {channel.id}",
      "thread_id: {thread.id}",
      "workspace_dir: {workspace.dir}",
      "skills_dir: {scope.skills}",
      "[/joi handoff v1]"
    ],
    "firstTurnPrefix": [
      "[joi bootstrap]",
      "Read your skill from {agent.bundle}/SKILL.md.",
      "Use `joi event append` for user-visible replies and handoff.",
      "Use `joi workspace read/write` for the per-thread feedback queue under {workspace.dir}/feedback-queue.json.",
      "Use `joi artifact publish` for feedback batches and per-item fix evidence.",
      "Use `joi action request` for human approval before pushing each fix.",
      "Each accepted item should hand off to `delivery` in a dedicated thread; do not modify code yourself.",
      "[/joi bootstrap]"
    ],
    "everyTurnSuffix": [
      "When finished, emit the configured Joi completion sentinel."
    ]
  }
}
