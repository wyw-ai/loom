{
  "actor": {
    "id": "actor_router",
    "kind": "agent",
    "displayName": "路由"
  },
  "transport": {
    "kind": "interactive_command",
    "command": "claude",
    "args": [],
    "env": {},
    "model": "claude-sonnet-4.6",
    "provider": {
      "kind": "claude",
      "settings": {
        "mode": "actor_profile"
      }
    },
    "interactive": {
      "session": {
        "idStrategy": "joi_uuid_per_scope",
        "newArgs": [
          "--print",
          "--permission-mode",
          "bypassPermissions",
          "{prompt}",
          "--session-id",
          "{session_id}"
        ],
        "resumeArgs": [
          "--print",
          "--permission-mode",
          "bypassPermissions",
          "{prompt}",
          "--resume",
          "{session_id}"
        ],
        "onMissing": "create",
        "onSignatureChanged": "create",
        "onResumeFailed": "fail"
      },
      "prompt": {
        "template": "{joi_envelope}",
        "completionContract": {
          "sentinel": "__JOI_DONE__",
          "instruction": "When your final user-visible answer is complete, output __JOI_DONE__ on a line by itself. Do not output anything after it."
        }
      },
      "completion": {
        "detect": "sentinel",
        "stripSentinel": true,
        "idleTimeoutMs": 30000,
        "maxTurnMs": 600000
      },
      "output": {
        "stripAnsi": true,
        "stderr": "trace",
        "streamPartial": true
      },
      "kill": {
        "onComplete": {
          "action": "sigterm",
          "graceMs": 3000,
          "fallback": "sigkill"
        },
        "onCancel": {
          "action": "sigterm",
          "graceMs": 1000,
          "fallback": "sigkill"
        },
        "onTimeout": {
          "action": "sigkill"
        }
      }
    }
  },
  "autostart": false,
  "bundle": {
    "source": "data/agents/router/bundle",
    "version": "v1",
    "installMode": "copy",
    "root": "{agent.root}/bundles",
    "current": "{agent.root}/bundles/current"
  },
  "handoff": {
    "triggerPromptPrefix": "/router\n",
    "applyOn": "every-turn"
  },
  "promptTemplate": {
    "activeSkill": "router",
    "everyTurnPrefix": [
      "[joi handoff v1]",
      "scope_kind: {scope.kind}",
      "scope_id: {scope.id}",
      "actor_id: {actor.id}",
      "from_actor: {trigger.actor_id}",
      "trigger_event_id: {trigger.id}",
      "channel_id: {channel.id}",
      "thread_id: {thread.id}",
      "workspace_dir: {workspace.dir}",
      "skills_dir: {scope.skills}",
      "[/joi handoff v1]"
    ],
    "firstTurnPrefix": [
      "[joi bootstrap]",
      "你的技能定义如下，请严格遵守：",
      "----- BEGIN SKILL -----",
      "{agent.skillBody}",
      "----- END SKILL -----",
      "Use `joi event append` (or `joi say` / `joi handoff`) for user-visible replies and handoff.",
      "Do not embed the callee's slash command in your reply text; runtime injects it.",
      "[/joi bootstrap]"
    ],
    "everyTurnSuffix": [
      "When finished, emit the configured Joi completion sentinel."
    ]
  }
}
